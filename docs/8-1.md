# [Phase 8] 사이클 8-1 — 메인 메뉴 렌더링

`PLAN.md`의 Phase 8 [8-1] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

`ConsoleView.render_main_menu(summary)`가 5개 메뉴 항목과 전체 시료 요약 정보(등록
시료 수, 재고 합계)를 포함한 문자열을 반환한다. (PRD.md 3절)

`PoC/ConsoleMVC`에서 검증한 원칙과 동일하게, View는 Controller가 준 순수 `dict`만으로
렌더링하며 Model을 알지 못한다.

### 작성할 실패 테스트

`tests/view/test_console_view.py` (신규 파일)

```python
from view.console_view import ConsoleView


def test_메인_메뉴는_5개_항목과_시료_요약_정보를_표시한다():
    view = ConsoleView()

    output = view.render_main_menu({"sample_count": 3, "total_stock": 42})

    assert "시료 관리" in output
    assert "주문" in output
    assert "모니터링" in output
    assert "출고 처리" in output
    assert "생산 라인" in output
    assert "3" in output
    assert "42" in output
```

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `src/view/console_view.py` (신규): `ConsoleView`
  - `render_main_menu(self, summary)`: `summary["sample_count"]`, `summary["total_stock"]`와
    5개 메뉴 항목(시료 관리 / 주문 / 모니터링 / 출고 처리 / 생산 라인)을 포함한 문자열을
    조립해 반환한다.
- Controller/main.py 연동은 이후 사이클([8-2]~[8-7])에서 다룬다 — 이번 사이클은 View
  단위 테스트로 렌더링 로직만 검증한다.

### 검증 방법

```bash
pytest tests/view/test_console_view.py -v
```

→ `src/view/console_view.py`가 아직 없으므로 `ModuleNotFoundError`로 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/view/test_console_view.py -v
ERROR collecting tests/view/test_console_view.py
ModuleNotFoundError: No module named 'view'
```

`src/view` 패키지가 아직 없어 예상대로 실패함을 확인했다 (오타가 아니라 기능 부재로 인한 실패).

## GREEN

`src/view/console_view.py`를 신규 추가했다.

```python
class ConsoleView:
    def render_main_menu(self, summary):
        lines = [
            "=== 반도체 시료 생산주문관리 시스템 ===",
            f"등록 시료 수: {summary['sample_count']}개 | 총 재고: {summary['total_stock']}개",
            "1. 시료 관리",
            "2. 주문 (접수 / 승인 / 거절)",
            "3. 모니터링",
            "4. 출고 처리",
            "5. 생산 라인",
        ]
        return "\n".join(lines)
```

### GREEN 검증 결과

```bash
$ pytest -v
tests/view/test_console_view.py::test_메인_메뉴는_5개_항목과_시료_요약_정보를_표시한다 PASSED
... (기존 56개) PASSED

============================= 57 passed in 0.11s ==============================
```

전체 테스트(57개)가 모두 통과하고 회귀 없음을 확인했다.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- PLAN.md Phase 8 [8-1] 범위를 벗어난 구현 없음 확인 (Controller/main.py 연동은 [8-2]~[8-7]로 분리)
- 리팩토링 요청 없음
