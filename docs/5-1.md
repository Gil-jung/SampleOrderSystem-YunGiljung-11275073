# [Phase 5] 사이클 5-1 — 부족분 계산

`PLAN.md`의 Phase 5 [5-1] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

`부족분 = 주문 수량 - 승인 시점 가용 재고`를 계산하는 순수 함수. (PRD.md 4.5절)

### 작성할 실패 테스트

`tests/service/test_production_service.py` (신규 파일)

```python
from service.production_service import calculate_shortage


def test_부족분은_주문_수량에서_가용_재고를_뺀_값이다():
    assert calculate_shortage(order_quantity=5, available_stock=2) == 3
```

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `src/service/production_service.py`에 모듈 수준 순수 함수
  `calculate_shortage(order_quantity, available_stock)`를 추가한다.
  `return order_quantity - available_stock`.
- `ProductionService` 클래스와 독립적인 순수 계산 함수로 둔다 — [5-2]의
  실 생산량 계산, [5-4]의 큐 등록 로직에서 조합해 사용할 예정이다.

### 검증 방법

```bash
pytest tests/service/test_production_service.py -v
```

→ `calculate_shortage`가 아직 없으므로 `ImportError`로 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/service/test_production_service.py -v
ERROR collecting tests/service/test_production_service.py
ImportError: cannot import name 'calculate_shortage' from 'service.production_service'
```

`calculate_shortage`가 아직 없어 예상대로 실패함을 확인했다 (오타가 아니라 기능 부재로 인한 실패).

## GREEN

_(다음 단계에서 기록 예정)_

## REVIEW

_(GREEN 완료 후 기록 예정)_
