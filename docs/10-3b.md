# [Phase 10] 사이클 10-3b — 모니터링 서브메뉴: 재고량 조회

`PLAN.md`의 Phase 10 [10-3b] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

모니터링 서브메뉴에서 `2`(재고량 조회)를 선택하면 시료별 재고 수량과 상태(여유/부족/고갈)가
콘솔에 출력된다.

### 작성할 실패 테스트

`tests/test_main.py`에 아래 테스트를 추가한다.

```python
def test_모니터링_메뉴에서_재고량_조회_시_시료별_상태가_출력된다():
    user_input = "\n".join(
        [
            "1",  # 시료 관리
            "1",  # 등록
            "SMP-001",
            "Wafer-A",
            "2.5",
            "0.9",
            "0",
            "3",  # 모니터링
            "2",  # 재고량 조회
            "0",
            "0",
            "",
        ]
    )

    result = subprocess.run(
        [sys.executable, "main.py"],
        input=user_input,
        capture_output=True,
        text=True,
        cwd=SRC_DIR,
    )

    assert result.returncode == 0
    assert result.stderr == ""
    assert "SMP-001" in result.stdout
    assert "여유" in result.stdout
```

미출고(`CONFIRMED`) 수요가 없는 상태이므로 [6-6]의 경계 케이스 규칙에 따라 재고 0인
`SMP-001`도 "여유"로 판정된다.

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `ConsoleView.render_stock_statuses(statuses)`: `get_stock_statuses()`가 반환한 `dict`
  리스트를 `f"[{sample_id}] 재고: {stock} ({status})"` 형태로 렌더링.
- `main.py`의 `handle_monitoring_menu`에 `elif choice == "2":` 분기를 추가해
  `controller.get_stock_statuses()`를 `view.render_stock_statuses(...)`로 출력한다.

### 검증 방법

```bash
pytest tests/test_main.py -v
```

→ `"2"` 분기가 아직 없어 재고 정보가 출력되지 않아 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/test_main.py -v
FAILED tests/test_main.py::test_모니터링_메뉴에서_재고량_조회_시_시료별_상태가_출력된다
AssertionError: assert 'SMP-001' in '...(재고량 조회 분기 없이 무시됨)...'

1 failed, 8 passed in 0.60s
```

`handle_monitoring_menu`가 아직 `"2"` 분기를 처리하지 않아 예상대로 실패함을
확인했다 (오타가 아니라 기능 부재로 인한 실패).

## GREEN

`ConsoleView.render_stock_statuses(statuses)`를 추가하고, `handle_monitoring_menu`에
`elif choice == "2":` 분기를 추가했다.

### GREEN 검증 결과

```bash
$ pytest -v
tests/test_main.py::test_모니터링_메뉴에서_재고량_조회_시_시료별_상태가_출력된다 PASSED
... (기존 81개) PASSED

============================= 82 passed in 0.62s ==============================
```

전체 테스트(82개)가 모두 통과하고 회귀 없음을 확인했다. Phase 10 [10-3]
(모니터링 서브메뉴)의 주문량/재고량 조회가 모두 완료되었다.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- PLAN.md Phase 10 [10-3b] 범위를 벗어난 구현 없음 확인
- 리팩토링 요청 없음
- Phase 10 [10-3] (모니터링 서브메뉴) 전체 완료
