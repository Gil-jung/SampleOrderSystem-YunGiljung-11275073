# [Phase 9] 사이클 9-1 — 재고 충분 시나리오 (E2E, 통합 확인)

`PLAN.md`의 Phase 9 [9-1] 항목에 대한 tdd 스킬 사이클 기록.

## RED 이전 검토 — 이 사이클도 진짜 RED를 만들 수 없다

목표: 시료 등록(재고 임의 확보) → 예약 → 승인(재고 충분 → 즉시 `CONFIRMED`) →
출고(`RELEASE`) 전체 흐름이 `MenuController`를 통해 끊김 없이 동작한다.

이 흐름을 구성하는 개별 조각은 이미 각 사이클에서 개별적으로 검증되어 있다:

- 등록: [8-2]
- 예약: [8-3]
- 승인(재고 충분): [4-2], [8-3]
- 출고: [7-1], [8-6] (`test_출고_명령이_ReleaseService로_위임된다`가 이미
  예약→승인→출고 체인 자체를 검증했다. 차이는 [8-6]이 시료를 `sample_repository.add()`로
  직접 넣은 반면, 이번 사이클은 `controller.register_sample()`로 등록한다는 점뿐이다.)

따라서 새로 이 시나리오를 조립해도 프로덕션 코드 변경 없이 처음부터 통과할 가능성이
매우 높다 — [2-5]/[3-3]/[4-5]/[4-7]/[5-4]/[6-2]/[6-6]/[7-2]와 동일한 패턴이다.

### 제안

- 새로운 프로덕션 코드 변경은 없다.
- Phase 9는 `PoC/ConsoleMVC`의 `test_order_scenario.py`처럼 "전체 계층이 실제로 맞물려
  동작하는가"를 문서화하고 회귀를 방지하는 것이 목적이므로, 개별 사이클에서 이미
  검증된 조각이라도 **하나의 E2E 테스트로 다시 조립해 확인**하는 것 자체가 가치 있다.
  RED 없이 확인 테스트로 추가한다.
- `PLAN.md`에는 [9-1]이 기존 사이클들의 조합으로 이미 충족되었다는 메모를 추가한다.

### 확인 테스트 (참고용 — RED 없이 바로 통과 예정)

`tests/test_e2e_scenarios.py` (신규 파일)

```python
from model.order import OrderStatus
from repository.order_repository import OrderRepository
from repository.sample_repository import SampleRepository
from service.monitoring_service import MonitoringService
from service.order_service import OrderService
from service.production_service import ProductionService
from service.release_service import ReleaseService
from service.sample_service import SampleService
from controller.menu_controller import MenuController


def _build_controller():
    sample_repository = SampleRepository()
    order_repository = OrderRepository()
    production_service = ProductionService()
    sample_service = SampleService(sample_repository)
    order_service = OrderService(order_repository, sample_repository, production_service)
    monitoring_service = MonitoringService(order_repository, sample_repository)
    release_service = ReleaseService(order_repository)
    controller = MenuController(
        sample_service, order_service, monitoring_service, production_service, release_service
    )
    return controller, sample_repository, order_repository


def test_재고_충분_시나리오_등록부터_출고까지_끊김없이_동작한다():
    controller, sample_repository, order_repository = _build_controller()
    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )
    sample_repository.get("SMP-001").stock = 10  # 재고 임의 확보

    order_id = controller.reserve_order(
        sample_id="SMP-001", customer_name="홍길동", quantity=5
    )
    controller.approve_order(order_id)
    controller.release_order(order_id)

    released_order = order_repository.get(order_id)
    assert released_order.status == OrderStatus.RELEASE
    assert sample_repository.get("SMP-001").stock == 5
```

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

## 확인 (GREEN 상당)

`tests/test_e2e_scenarios.py`를 신규 추가했다. 프로덕션 코드 변경은 없다.

### 실행 결과

```bash
$ pytest -v
tests/test_e2e_scenarios.py::test_재고_충분_시나리오_등록부터_출고까지_끊김없이_동작한다 PASSED
... (기존 70개) PASSED

============================= 71 passed in 0.25s ==============================
```

예상대로 프로덕션 코드 변경 없이 즉시 통과했다. 전체 테스트(71개) 회귀 없음.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- RED 없이 확인 테스트만 추가하는 방식에 동의 확인
- 리팩토링 요청 없음
