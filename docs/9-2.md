# [Phase 9] 사이클 9-2 — 재고 부족 시나리오 (E2E, 통합 확인)

`PLAN.md`의 Phase 9 [9-2] 항목에 대한 tdd 스킬 사이클 기록.

## RED 이전 검토 — 이 사이클도 진짜 RED를 만들 수 없다

목표: 시료 등록(재고 0) → 예약 → 승인(재고 부족 → `PRODUCING`, 큐 등록) → 생산 완료
(`CONFIRMED`) → 출고(`RELEASE`) 전체 흐름이 끊김 없이 동작한다.

이 흐름의 각 조각은 이미 검증되어 있다: 승인(재고 부족)·큐 등록 [4-3], 생산 완료
[5-7], 출고 [7-1]/[8-6]. [9-1]과 동일한 이유로, 새로 조립해도 프로덕션 코드 변경
없이 통과할 가능성이 매우 높다.

### 설계 메모 — `complete_production`의 위치

`OrderService.complete_production()`은 [5-7]에서 이미 구현되어 있지만, PRD.md의
생산 라인 메뉴(1. 생산 현황 표기, 2. 대기 주문 확인)에는 "생산 완료 처리"라는
사용자 메뉴 항목이 없다 — [5-7] 작성 당시 메모한 대로, 생산 완료는 시스템(생산 라인)
이벤트로 간주하고 콘솔 메뉴에 노출하지 않기로 했다. 따라서 이 E2E 테스트에서는
`order_service.complete_production()`을 직접 호출한다 (다른 단계는 `MenuController`를
통해 사용자 관점으로 호출). 이를 위해 기존 [9-1]의 `_build_controller()` 헬퍼가
`order_service`도 함께 반환하도록 확장한다 ([9-1] 테스트도 새 반환 형태에 맞춰 조정).

### 제안

- 새로운 프로덕션 코드 변경은 없다.
- RED 없이 확인 테스트로 추가한다.
- `PLAN.md`에는 [9-2]가 기존 사이클들의 조합으로 이미 충족되었다는 메모를 추가한다.

### 확인 테스트 (참고용 — RED 없이 바로 통과 예정)

`tests/test_e2e_scenarios.py`

```python
def _build_controller():
    sample_repository = SampleRepository()
    order_repository = OrderRepository()
    production_service = ProductionService()
    sample_service = SampleService(sample_repository)
    order_service = OrderService(order_repository, sample_repository, production_service)
    monitoring_service = MonitoringService(order_repository, sample_repository)
    release_service = ReleaseService(order_repository)
    controller = MenuController(
        sample_service, order_service, monitoring_service, production_service, release_service
    )
    return controller, sample_repository, order_repository, order_service


def test_재고_부족_시나리오_생산_완료를_거쳐_출고까지_끊김없이_동작한다():
    controller, sample_repository, order_repository, order_service = _build_controller()
    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )
    # 재고는 등록 직후 기본값 0

    order_id = controller.reserve_order(
        sample_id="SMP-001", customer_name="홍길동", quantity=9
    )
    controller.approve_order(order_id)  # 재고 부족 -> PRODUCING, 큐 등록

    assert order_repository.get(order_id).status == OrderStatus.PRODUCING
    assert controller.get_production_queue() == [order_id]

    order_service.complete_production()  # 생산 완료 -> CONFIRMED

    assert order_repository.get(order_id).status == OrderStatus.CONFIRMED
    assert controller.get_production_queue() == []

    controller.release_order(order_id)

    released_order = order_repository.get(order_id)
    assert released_order.status == OrderStatus.RELEASE
    assert sample_repository.get("SMP-001").stock == 0
```

기존 [9-1] 테스트의 `_build_controller()` 호출부도 반환값 4개(`order_service` 추가)에
맞춰 조정한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

## 확인 (GREEN 상당)

`tests/test_e2e_scenarios.py`에 확인 테스트를 추가했다 (`_build_controller()`가
`order_service`도 반환하도록 확장, [9-1] 테스트도 함께 조정). 프로덕션 코드 변경은 없다.

### 실행 결과

```bash
$ pytest -v
tests/test_e2e_scenarios.py::test_재고_부족_시나리오_생산_완료를_거쳐_출고까지_끊김없이_동작한다 PASSED
... (기존 71개) PASSED

============================= 72 passed in 0.28s ==============================
```

예상대로 프로덕션 코드 변경 없이 즉시 통과했다. 전체 테스트(72개) 회귀 없음.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- RED 없이 확인 테스트만 추가하는 방식에 동의 확인
- 리팩토링 요청 없음
