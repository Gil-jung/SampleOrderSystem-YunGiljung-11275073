# [Phase 10] 사이클 10-2d — 주문 서브메뉴: 거절

`PLAN.md`의 Phase 10 [10-2d] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

주문 서브메뉴에서 `4`(거절)를 선택하고 `order_id`를 입력하면 주문이 거절되고,
접수된 목록 조회에서 더 이상 표시되지 않는다.

### 작성할 실패 테스트

`tests/test_main.py`에 아래 테스트를 추가한다.

```python
def test_주문_메뉴에서_거절하면_접수된_목록에서_사라진다():
    user_input = "\n".join(
        [
            "1",  # 시료 관리
            "1",  # 등록
            "SMP-001",
            "Wafer-A",
            "2.5",
            "0.9",
            "0",
            "2",  # 주문
            "1",  # 예약
            "SMP-001",
            "홍길동",
            "5",
            "4",  # 거절
            "ORD-0001",
            "2",  # 접수된 목록 조회
            "0",
            "0",
            "",
        ]
    )

    result = subprocess.run(
        [sys.executable, "main.py"],
        input=user_input,
        capture_output=True,
        text=True,
        cwd=SRC_DIR,
    )

    assert result.returncode == 0
    assert result.stderr == ""
    assert "주문이 없습니다." in result.stdout
```

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `main.py`의 `handle_order_menu`에 `elif choice == "4":` 분기를 추가한다:
  `order_id`를 입력받아 `controller.reject_order(order_id)`를 호출하고
  `"거절 완료"`를 출력한다.

### 검증 방법

```bash
pytest tests/test_main.py -v
```

→ `"4"` 분기가 아직 없어 주문이 여전히 `RESERVED`로 남아, 접수된 목록 조회에
"주문이 없습니다."가 아니라 주문 내역이 출력되어 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/test_main.py -v
FAILED tests/test_main.py::test_주문_메뉴에서_거절하면_접수된_목록에서_사라진다
AssertionError: assert '주문이 없습니다.' in '...[ORD-0001] 홍길동 - SMP-001 x5 (RESERVED)...'

1 failed, 6 passed in 0.47s
```

`"4"` 분기가 아직 없어 주문이 여전히 `RESERVED`로 남아 예상대로 실패함을 확인했다
(오타가 아니라 기능 부재로 인한 실패).

## GREEN

`handle_order_menu`에 `elif choice == "4":` 분기를 추가했다.

### GREEN 검증 결과

```bash
$ pytest -v
tests/test_main.py::test_주문_메뉴에서_거절하면_접수된_목록에서_사라진다 PASSED
... (기존 79개) PASSED

============================= 80 passed in 0.51s ==============================
```

전체 테스트(80개)가 모두 통과하고 회귀 없음을 확인했다. 이로써 Phase 10 [10-2]
(주문 서브메뉴)의 예약/목록조회/승인/거절이 모두 완료되었다.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- PLAN.md Phase 10 [10-2d] 범위를 벗어난 구현 없음 확인
- 리팩토링 요청 없음
- Phase 10 [10-2] (주문 서브메뉴) 전체 완료
