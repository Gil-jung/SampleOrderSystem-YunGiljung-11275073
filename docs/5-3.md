# [Phase 5] 사이클 5-3 — 총 생산 시간 계산

`PLAN.md`의 Phase 5 [5-3] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

`총 생산 시간 = 평균 생산시간 × 실 생산량`을 계산하는 순수 함수. (PRD.md 4.5절)

### 작성할 실패 테스트

`tests/service/test_production_service.py`에 아래 테스트를 추가한다.

```python
from service.production_service import calculate_total_production_time


def test_총_생산_시간은_평균_생산시간에_실_생산량을_곱한_값이다():
    assert calculate_total_production_time(avg_production_time=2.5, actual_production=10) == 25.0
```

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `src/service/production_service.py`에 모듈 수준 순수 함수
  `calculate_total_production_time(avg_production_time, actual_production)`를 추가한다.
  `return avg_production_time * actual_production`.

### 검증 방법

```bash
pytest tests/service/test_production_service.py -v
```

→ `calculate_total_production_time`이 아직 없으므로 `ImportError`로 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/service/test_production_service.py -v
ERROR collecting tests/service/test_production_service.py
ImportError: cannot import name 'calculate_total_production_time' from 'service.production_service'
```

`calculate_total_production_time`이 아직 없어 예상대로 실패함을 확인했다 (오타가 아니라 기능 부재로 인한 실패).

## GREEN

_(다음 단계에서 기록 예정)_

## REVIEW

_(GREEN 완료 후 기록 예정)_
