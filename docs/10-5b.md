# [Phase 10] 사이클 10-5b — 생산 라인 서브메뉴: 대기 큐 조회

`PLAN.md`의 Phase 10 [10-5b] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

생산 라인 서브메뉴에서 `2`(대기 큐 조회)를 선택하면 생산 큐에 대기 중인 주문
ID 목록이 등록 순서(FIFO)대로 콘솔에 출력된다.

### 작성할 실패 테스트

`tests/test_main.py`에 아래 테스트를 추가한다.

```python
def test_생산_라인_메뉴에서_대기_큐를_조회하면_주문ID_목록이_출력된다():
    user_input = "\n".join(
        [
            "1",  # 시료 관리
            "1",  # 등록
            "SMP-001",
            "Wafer-A",
            "2.5",
            "0.9",
            "0",
            "2",  # 주문
            "1",  # 예약 1 (재고 0 -> 승인 시 PRODUCING)
            "SMP-001",
            "홍길동",
            "9",
            "1",  # 예약 2
            "SMP-001",
            "김철수",
            "3",
            "3",  # 승인
            "ORD-0001",
            "3",  # 승인
            "ORD-0002",
            "0",
            "5",  # 생산 라인
            "2",  # 대기 큐 조회
            "0",
            "0",
            "",
        ]
    )

    result = subprocess.run(
        [sys.executable, "main.py"],
        input=user_input,
        capture_output=True,
        text=True,
        cwd=SRC_DIR,
    )

    assert result.returncode == 0
    assert result.stderr == ""
    # 두 주문 ID 모두 "예약 완료" 메시지에서 한 번씩 출력되므로, 큐 목록 출력이 실제로
    # 동작해야만 각 ID가 2번씩(예약 완료 + 큐 목록) 등장한다. 등장 횟수까지 확인해야
    # 큐 조회 분기가 실행되었음을 진짜로 검증할 수 있다 (그렇지 않으면 예약 순서가
    # 우연히 같아 순서 비교만으로는 위양성이 발생한다).
    assert result.stdout.count("ORD-0001") == 2
    assert result.stdout.count("ORD-0002") == 2
    ord1_last_index = result.stdout.rindex("ORD-0001")
    ord2_last_index = result.stdout.rindex("ORD-0002")
    assert ord1_last_index < ord2_last_index
```

### 설계 오류 정정 (첫 시도)

처음에는 등장 순서(`index`/`rindex`)만 비교했는데, 큐 조회 분기가 아직 구현되지
않은 상태에서도 각 주문 ID가 "예약 완료" 메시지에서 예약한 순서대로 한 번씩만
출력되어 **테스트가 프로덕션 코드 변경 없이 즉시 통과**해버렸다 (위양성 — 실제로는
아무것도 검증하지 못함). 등장 횟수(`count`)까지 함께 확인하도록 수정해 진짜 RED를
확보했다.

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `ConsoleView.render_production_queue(queue)`: `queue`가 비어있으면
  `"대기 중인 주문이 없습니다."`, 아니면 각 `order_id`를 한 줄씩 렌더링.
- `main.py`의 `handle_production_menu`에 `elif choice == "2":` 분기를 추가해
  `controller.get_production_queue()`를 `view.render_production_queue(...)`로 출력한다.

### 검증 방법

```bash
pytest tests/test_main.py -v
```

→ `"2"` 분기가 아직 없어 큐 목록이 출력되지 않아 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/test_main.py -v
FAILED tests/test_main.py::test_생산_라인_메뉴에서_대기_큐를_조회하면_주문ID_목록이_출력된다
AssertionError: assert 1 == 2
```

수정된 테스트로 예상대로 실패함을 확인했다 (오타가 아니라 기능 부재로 인한 실패 —
큐 조회 분기가 없어 각 주문 ID가 1번만 등장).

## GREEN

`ConsoleView.render_production_queue(queue)`를 추가하고, `handle_production_menu`에
`elif choice == "2":` 분기를 추가했다.

### GREEN 검증 결과

```bash
$ pytest -v
tests/test_main.py::test_생산_라인_메뉴에서_대기_큐를_조회하면_주문ID_목록이_출력된다 PASSED
... (기존 86개) PASSED

============================= 87 passed in 0.94s ==============================
```

전체 테스트(87개)가 모두 통과하고 회귀 없음을 확인했다. Phase 10 [10-5]
(생산 라인 서브메뉴)가 모두 완료되었다.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- PLAN.md Phase 10 [10-5b] 범위를 벗어난 구현 없음 확인
- 리팩토링 요청 없음
- Phase 10 [10-5] (생산 라인 서브메뉴) 전체 완료
