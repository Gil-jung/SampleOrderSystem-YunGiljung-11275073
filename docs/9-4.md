# [Phase 9] 사이클 9-4 — FIFO 다중 생산 시나리오 (E2E, 통합 확인)

`PLAN.md`의 Phase 9 [9-4] 항목에 대한 tdd 스킬 사이클 기록.

## RED 이전 검토 — 이 사이클도 진짜 RED를 만들 수 없다

목표: 재고 부족 주문 2건 이상을 승인하면 생산 큐에 등록 순서대로 쌓이고, 생산 완료를
반복하면 등록 순서 그대로(FIFO) 완료 처리된다.

큐 등록 순서 보장은 [5-4]에서, 생산 완료 후 다음 대기 주문으로 진행하는 것은 [5-7]에서
이미 검증되어 있다. 이번 사이클은 두 조각을 이어붙여 "완료를 두 번 반복했을 때도 순서가
유지되는가"를 확인하는 것인데, 이는 동일한 FIFO 큐(`list.pop(0)`)와 `complete_production`
로직이 그대로 반복 적용되는 것뿐이라 새로운 분기가 필요 없다. [9-1]~[9-3]과 동일한
이유로 새로 조립해도 프로덕션 코드 변경 없이 통과할 가능성이 매우 높다.

### 제안

- 새로운 프로덕션 코드 변경은 없다.
- RED 없이 확인 테스트로 추가한다.
- `PLAN.md`에는 [9-4]가 [5-4]/[5-7]의 조합으로 이미 충족되었다는 메모를 추가한다.

### 확인 테스트 (참고용 — RED 없이 바로 통과 예정)

`tests/test_e2e_scenarios.py`

```python
def test_FIFO_다중_생산_시나리오_등록_순서대로_생산_완료된다():
    controller, sample_repository, order_repository, order_service = _build_controller()
    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )
    # 재고는 등록 직후 기본값 0

    first_id = controller.reserve_order(
        sample_id="SMP-001", customer_name="홍길동", quantity=9
    )
    second_id = controller.reserve_order(
        sample_id="SMP-001", customer_name="김철수", quantity=5
    )
    controller.approve_order(first_id)
    controller.approve_order(second_id)

    assert controller.get_production_queue() == [first_id, second_id]

    order_service.complete_production()  # first_id 완료

    assert order_repository.get(first_id).status == OrderStatus.CONFIRMED
    assert order_repository.get(second_id).status == OrderStatus.PRODUCING
    assert controller.get_production_queue() == [second_id]

    order_service.complete_production()  # second_id 완료

    assert order_repository.get(second_id).status == OrderStatus.CONFIRMED
    assert controller.get_production_queue() == []
```

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

## 확인 (GREEN 상당)

`tests/test_e2e_scenarios.py`에 확인 테스트를 추가했다. 프로덕션 코드 변경은 없다.

### 실행 결과

```bash
$ pytest -v
tests/test_e2e_scenarios.py::test_FIFO_다중_생산_시나리오_등록_순서대로_생산_완료된다 PASSED
... (기존 73개) PASSED

============================= 74 passed in 0.28s ==============================
```

예상대로 프로덕션 코드 변경 없이 즉시 통과했다. 전체 테스트(74개) 회귀 없음.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- RED 없이 확인 테스트만 추가하는 방식에 동의 확인
- 리팩토링 요청 없음
- Phase 9(종합 시나리오 검증) 전체 사이클([9-1]~[9-4]) 완료
- **PLAN.md 전체 Phase(0~9) 완료**
