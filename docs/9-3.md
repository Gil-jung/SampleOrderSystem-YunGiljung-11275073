# [Phase 9] 사이클 9-3 — 거절 시나리오 (E2E, 통합 확인)

`PLAN.md`의 Phase 9 [9-3] 항목에 대한 tdd 스킬 사이클 기록.

## RED 이전 검토 — 이 사이클도 진짜 RED를 만들 수 없다

목표: 예약 → 거절(`REJECTED`) → 모니터링 집계에서 제외됨을 확인한다.

이 흐름은 [6-2]에서 이미 정확히 같은 내용을 검증했다
(`test_REJECTED_주문은_집계에서_제외된다`) — 차이는 [6-2]가 Service를 직접
사용하고, 이번 사이클은 `MenuController`를 통해 사용자 관점으로 동일한 흐름을
검증한다는 점뿐이다. [9-1]/[9-2]와 동일한 이유로 새로 조립해도 프로덕션 코드
변경 없이 통과할 가능성이 매우 높다.

### 제안

- 새로운 프로덕션 코드 변경은 없다.
- RED 없이 확인 테스트로 추가한다.
- `PLAN.md`에는 [9-3]이 [6-2]로 이미 충족되었다는 메모를 추가한다.

### 확인 테스트 (참고용 — RED 없이 바로 통과 예정)

`tests/test_e2e_scenarios.py`

```python
def test_거절_시나리오_예약_후_거절하면_모니터링_집계에서_제외된다():
    controller, sample_repository, order_repository, order_service = _build_controller()
    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )

    order_id = controller.reserve_order(
        sample_id="SMP-001", customer_name="홍길동", quantity=1
    )
    controller.reject_order(order_id)

    assert order_repository.get(order_id).status == OrderStatus.REJECTED
    counts = controller.get_order_counts()
    assert sum(counts.values()) == 0
```

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

## 확인 (GREEN 상당)

`tests/test_e2e_scenarios.py`에 확인 테스트를 추가했다. 프로덕션 코드 변경은 없다.

### 실행 결과

```bash
$ pytest -v
tests/test_e2e_scenarios.py::test_거절_시나리오_예약_후_거절하면_모니터링_집계에서_제외된다 PASSED
... (기존 72개) PASSED

============================= 73 passed in 0.28s ==============================
```

예상대로 프로덕션 코드 변경 없이 즉시 통과했다. 전체 테스트(73개) 회귀 없음.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- RED 없이 확인 테스트만 추가하는 방식에 동의 확인
- 리팩토링 요청 없음
