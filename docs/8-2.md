# [Phase 8] 사이클 8-2 — 시료 관리 메뉴 흐름

`PLAN.md`의 Phase 8 [8-2] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작 — 등록/조회/검색 3개 명령의 위임)

`MenuController`의 시료 관리 명령(등록/조회/검색)이 `SampleService`로 올바르게
위임되고, View가 그릴 수 있는 순수 `dict`(또는 `dict` 리스트)로 결과를 반환한다.
(PRD.md 3절 — `PoC/ConsoleMVC` 원칙: Controller는 Model 객체가 아니라 `dict`만
View에 넘긴다.)

### 작성할 실패 테스트

`tests/controller/test_menu_controller.py` (신규 파일)

```python
from repository.sample_repository import SampleRepository
from service.sample_service import SampleService
from controller.menu_controller import MenuController


def test_시료_등록_명령이_SampleService로_위임된다():
    repository = SampleRepository()
    sample_service = SampleService(repository)
    controller = MenuController(sample_service)

    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )

    stored = repository.get("SMP-001")
    assert stored.name == "Wafer-A"
    assert stored.stock == 0


def test_시료_목록_조회_명령이_dict_리스트로_반환된다():
    repository = SampleRepository()
    sample_service = SampleService(repository)
    controller = MenuController(sample_service)
    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )

    result = controller.list_samples()

    assert result == [{"sample_id": "SMP-001", "name": "Wafer-A", "stock": 0}]


def test_시료_검색_명령이_dict_리스트로_반환된다():
    repository = SampleRepository()
    sample_service = SampleService(repository)
    controller = MenuController(sample_service)
    controller.register_sample(
        sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9
    )
    controller.register_sample(
        sample_id="SMP-002", name="Other-B", avg_production_time=1.0, yield_rate=0.8
    )

    result = controller.search_samples("Wafer")

    assert result == [{"sample_id": "SMP-001", "name": "Wafer-A", "stock": 0}]
```

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `src/controller/menu_controller.py` (신규): `MenuController`
  - `__init__(self, sample_service)` — 이번 사이클은 시료 관리만 다루므로 다른 Service는
    아직 받지 않는다. 주문/모니터링/생산/출고 관련 Service는 해당 사이클([8-3]~[8-6])에서
    생성자에 추가한다.
  - `register_sample(sample_id, name, avg_production_time, yield_rate)`:
    `sample_service.register(...)` 그대로 위임.
  - `list_samples()`: `sample_service.list_all()` 결과를
    `[{"sample_id": s.sample_id, "name": s.name, "stock": s.stock} for s in ...]`로 변환.
  - `search_samples(name)`: `sample_service.search(name)` 결과를 동일한 방식으로 변환.

### 검증 방법

```bash
pytest tests/controller/test_menu_controller.py -v
```

→ `src/controller/menu_controller.py`가 아직 없으므로 `ModuleNotFoundError`로 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/controller/test_menu_controller.py -v
ERROR collecting tests/controller/test_menu_controller.py
ModuleNotFoundError: No module named 'controller'
```

`src/controller` 패키지가 아직 없어 예상대로 실패함을 확인했다 (오타가 아니라 기능 부재로 인한 실패).

## GREEN

`src/controller/menu_controller.py`를 신규 추가했다.

```python
class MenuController:
    def __init__(self, sample_service):
        self._sample_service = sample_service

    def register_sample(self, sample_id, name, avg_production_time, yield_rate):
        self._sample_service.register(sample_id, name, avg_production_time, yield_rate)

    def list_samples(self):
        return [self._to_dict(sample) for sample in self._sample_service.list_all()]

    def search_samples(self, name):
        return [self._to_dict(sample) for sample in self._sample_service.search(name)]

    @staticmethod
    def _to_dict(sample):
        return {
            "sample_id": sample.sample_id,
            "name": sample.name,
            "stock": sample.stock,
        }
```

### GREEN 검증 결과

```bash
$ pytest -v
tests/controller/test_menu_controller.py::test_시료_등록_명령이_SampleService로_위임된다 PASSED
tests/controller/test_menu_controller.py::test_시료_목록_조회_명령이_dict_리스트로_반환된다 PASSED
tests/controller/test_menu_controller.py::test_시료_검색_명령이_dict_리스트로_반환된다 PASSED
... (기존 57개) PASSED

============================= 60 passed in 0.13s ==============================
```

전체 테스트(60개)가 모두 통과하고 회귀 없음을 확인했다.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- PLAN.md Phase 8 [8-2] 범위를 벗어난 구현 없음 확인 (다른 Service 연동은 [8-3]~[8-6]으로 분리)
- 리팩토링 요청 없음
