# [Phase 6] 사이클 6-1 — 상태별 주문 수 집계

`PLAN.md`의 Phase 6 [6-1] 항목에 대한 tdd 스킬 사이클 기록.

## RED

### 목표 (단일 동작)

`MonitoringService.count_by_status()`가 `RESERVED`/`CONFIRMED`/`PRODUCING`/`RELEASE`
각 상태의 주문 수를 집계해 반환한다. (PRD.md 4.4절)

`REJECTED`를 명시적으로 제외하는 것은 [6-2]에서 별도로 다룬다. 이번 사이클에서는
정상 흐름 4개 상태의 집계 자체가 정확한지만 검증한다.

### 작성할 실패 테스트

`tests/service/test_monitoring_service.py` (신규 파일)

```python
from model.order import OrderStatus
from model.sample import Sample
from repository.order_repository import OrderRepository
from repository.sample_repository import SampleRepository
from service.monitoring_service import MonitoringService
from service.order_service import OrderService
from service.production_service import ProductionService


def test_상태별_주문_수를_집계한다():
    order_repository = OrderRepository()
    sample_repository = SampleRepository()
    sample_repository.add(
        Sample(sample_id="SMP-001", name="Wafer-A", avg_production_time=2.5, yield_rate=0.9)
    )
    sample_repository.get("SMP-001").stock = 10
    order_service = OrderService(order_repository, sample_repository, ProductionService())
    monitoring_service = MonitoringService(order_repository)

    order_service.reserve(sample_id="SMP-001", customer_name="홍길동", quantity=1)
    order_service.reserve(sample_id="SMP-001", customer_name="김철수", quantity=1)
    confirmed_id = order_service.reserve(sample_id="SMP-001", customer_name="이영희", quantity=1)
    order_service.approve(confirmed_id)  # 재고 충분 -> CONFIRMED

    counts = monitoring_service.count_by_status()

    assert counts[OrderStatus.RESERVED] == 2
    assert counts[OrderStatus.CONFIRMED] == 1
    assert counts[OrderStatus.PRODUCING] == 0
    assert counts[OrderStatus.RELEASE] == 0
```

### 구현 방향 (GREEN 예고 — RED 단계에서는 작성하지 않음)

- `src/service/monitoring_service.py` (신규): `MonitoringService`
  - `__init__(self, order_repository)`
  - `count_by_status()`: `RESERVED`/`CONFIRMED`/`PRODUCING`/`RELEASE` 4개 키를 0으로
    초기화한 `dict`를 만들고, `order_repository.list()`를 순회하며 상태별로 개수를 누적한다.

### 검증 방법

```bash
pytest tests/service/test_monitoring_service.py -v
```

→ `src/service/monitoring_service.py`가 아직 없으므로 `ModuleNotFoundError`로 실패하는 것을 확인한다.

### 승인 상태

- [x] 사람 파트너 승인 완료 (2026-07-15)

### RED 검증 결과

```bash
$ pytest tests/service/test_monitoring_service.py -v
ERROR collecting tests/service/test_monitoring_service.py
ModuleNotFoundError: No module named 'service.monitoring_service'
```

`src/service/monitoring_service.py`가 아직 없어 예상대로 실패함을 확인했다
(오타가 아니라 기능 부재로 인한 실패).

## GREEN

`src/service/monitoring_service.py`를 신규 추가했다.

```python
from model.order import OrderStatus

_MONITORED_STATUSES = [
    OrderStatus.RESERVED,
    OrderStatus.CONFIRMED,
    OrderStatus.PRODUCING,
    OrderStatus.RELEASE,
]


class MonitoringService:
    def __init__(self, order_repository):
        self._order_repository = order_repository

    def count_by_status(self):
        counts = {status: 0 for status in _MONITORED_STATUSES}
        for order in self._order_repository.list():
            if order.status in counts:
                counts[order.status] += 1
        return counts
```

### GREEN 검증 결과

```bash
$ pytest -v
tests/service/test_monitoring_service.py::test_상태별_주문_수를_집계한다 PASSED
... (기존 47개) PASSED

============================= 48 passed in 0.05s ==============================
```

전체 테스트(48개)가 모두 통과하고 회귀 없음을 확인했다.

## REVIEW

- 사람 파트너 리뷰 승인 완료 (2026-07-15)
- PLAN.md Phase 6 [6-1] 범위를 벗어난 구현 없음 확인
- 리팩토링 요청 없음
