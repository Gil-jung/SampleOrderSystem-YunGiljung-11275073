[project]
name = "dummy-data-generator"
version = "0.1.0"
description = "Console tool for generating test dummy data as JSON"
requires-python = ">=3.10"

[project.scripts]
dummy-gen = "dummygen.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
